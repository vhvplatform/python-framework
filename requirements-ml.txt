# Optional ML dependencies for SaaS Framework
# Install with: pip install -r requirements.txt -r requirements-ml.txt
#
# WARNING: ML dependencies are very large (several GB download)
# Ensure you have sufficient disk space (~10GB free) before installing

# Machine Learning
torch>=2.1.0,<3.0.0           # ~2GB download
transformers>=4.35.0,<5.0.0   # ~1GB with models
mlflow>=2.9.0,<3.0.0
onnxruntime>=1.16.0,<2.0.0    # ~100MB
